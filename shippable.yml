# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
  pre_ci:
    - docker build -t=shippabledocker/hitchcock3_sample_node .

  pre_ci_boot:
    image_name: shippabledocker/hitchcock3_sample_node
    image_tag: latest
    pull: false
    options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'

  ci:
    - ps -eax
    - ls -al

  post_ci:
    - docker push shippabledocker/hitchcock3_sample_node

integrations:
  hub:
    - integrationName: ship-docker
      type: docker
     # branches:
      #  only:
       #   - master
  notifications:
        - integrationName: email
          type: email
          recipients:
            - shiphitchcock3@gmail.com
           # - exampletwo@org.com
          #branches:
           # only:
            #    - master
             #   - dev
          on_success: always
          on_failure: always 
          
        - integrationName: ship-slack
          type: slack
          recipients:
           # - "#public-chnlll344"
            - @channel
          #branches:
           #   only:
            #    - master
             #   - dev
          on_success: always
          on_failure: always 
        - integrationName: irc
          type: irc
          recipients:
            #- "chat.freenode.net#test-ircopd3939"
            - "@chat.freenode.net#test3-irc"
         # branches:
          #  only:
             #   - master
               # - test
          on_start: always
          on_success: always
          on_failure: always 
          
          

