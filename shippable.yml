# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:

  ci:
    - ps -eax
    - ls -al
    #- if [ "$SHIPPABLE_NODE_VERSION" == "0.12" ]; then foobar; fi

integrations:
  notifications:
        - integrationName: email
          type: email
          recipients:
            - abhinava@shippable.com
          on_start: always
          on_pull_request: always
          on_success: always
          on_failure: always 
          
        - integrationName: irc
          type: irc
          recipients:
            - "chat.freenode.net#shipIRC"
          on_start: always
          on_pull_request: always
          on_success: always
          on_failure: always 

